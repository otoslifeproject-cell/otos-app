diff --git a/signals.html b/signals.html
index 3333333..4444444 100644
--- a/signals.html
+++ b/signals.html
@@ -1,102 +1,270 @@
 <!DOCTYPE html>
 <html lang="en">
 <head>
   <meta charset="UTF-8" />
-  <title>Signals</title>
+  <title>OTOS Signals</title>
   <meta name="viewport" content="width=device-width, initial-scale=1.0" />
   <style>
     :root{
       --bg:#0f1115;
       --panel:#171a21;
-      --accent:#00e676;
+      --accent:#00e676;
       --text:#e6e6e6;
       --muted:#9aa0a6;
+      --line:#242938;
+      --warn:#ffb300;
     }
     *{box-sizing:border-box;}
     body{
       margin:0;
       padding:20px;
       background:var(--bg);
       color:var(--text);
       font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,sans-serif;
     }
-    h1{margin:0 0 10px 0;}
-    .muted{color:var(--muted);}
-    .card{
-      background:var(--panel);
-      border-radius:14px;
-      padding:16px;
-      border:1px solid #2a2e38;
-      margin-top:14px;
-    }
-    .row{display:flex; gap:10px; flex-wrap:wrap; align-items:center;}
-    input,select,textarea{
-      width:100%;
-      background:var(--panel);
-      color:var(--text);
-      border:1px solid #2a2e38;
-      border-radius:10px;
-      padding:10px;
-      outline:none;
-    }
-    button{
-      border:none;
-      border-radius:12px;
-      padding:12px 16px;
-      background:var(--accent);
-      color:#04130a;
-      font-weight:700;
-      cursor:pointer;
-    }
-    button.secondary{
-      background:#2a2e38;
-      color:var(--text);
-      font-weight:600;
-    }
-    .list{margin-top:12px; display:grid; gap:10px;}
-    .item{
-      padding:12px;
-      border:1px solid #2a2e38;
-      border-radius:12px;
-      background:rgba(255,255,255,.02);
-    }
-    .pill{
-      display:inline-block;
-      padding:4px 8px;
-      border-radius:999px;
-      border:1px solid #2a2e38;
-      color:var(--muted);
-      font-size:12px;
-      margin-right:8px;
-    }
-    .small{font-size:12px; color:var(--muted);}
+    h1{margin:0 0 6px 0; font-size:22px;}
+    .topbar{
+      display:flex;
+      align-items:flex-start;
+      justify-content:space-between;
+      gap:16px;
+      margin-bottom:14px;
+    }
+    .muted{color:var(--muted);}
+    .small{font-size:12px; color:var(--muted);}
+    .grid{
+      display:grid;
+      grid-template-columns: 1.1fr 1.9fr;
+      gap:16px;
+      align-items:start;
+    }
+    @media (max-width: 980px){
+      .grid{grid-template-columns: 1fr;}
+    }
+    .card{
+      background:var(--panel);
+      border-radius:16px;
+      padding:16px;
+      border:1px solid var(--line);
+      box-shadow: 0 10px 30px rgba(0,0,0,.25);
+    }
+    .kpis{
+      display:grid;
+      grid-template-columns: repeat(3, 1fr);
+      gap:10px;
+      margin-top:12px;
+    }
+    .kpi{
+      padding:12px;
+      border-radius:14px;
+      border:1px solid var(--line);
+      background:rgba(255,255,255,.02);
+    }
+    .kpi .n{font-size:22px; font-weight:800; line-height:1;}
+    .kpi .l{margin-top:6px; font-size:12px; color:var(--muted);}
+    .controls{
+      display:grid;
+      grid-template-columns: 1fr 180px 160px;
+      gap:10px;
+      margin-top:12px;
+    }
+    @media (max-width: 720px){
+      .controls{grid-template-columns: 1fr;}
+    }
+    input,select{
+      width:100%;
+      background:rgba(0,0,0,.12);
+      color:var(--text);
+      border:1px solid var(--line);
+      border-radius:12px;
+      padding:10px 12px;
+      outline:none;
+    }
+    button{
+      border:none;
+      border-radius:12px;
+      padding:10px 12px;
+      background:var(--accent);
+      color:#04130a;
+      font-weight:800;
+      cursor:pointer;
+    }
+    button.secondary{
+      background:#2a2e38;
+      color:var(--text);
+      font-weight:700;
+    }
+    button.warn{
+      background:var(--warn);
+      color:#1a1200;
+      font-weight:900;
+    }
+    .table{
+      margin-top:12px;
+      border:1px solid var(--line);
+      border-radius:14px;
+      overflow:hidden;
+    }
+    .thead,.trow{
+      display:grid;
+      grid-template-columns: 160px 110px 1fr 220px;
+      gap:10px;
+      align-items:center;
+      padding:10px 12px;
+    }
+    .thead{
+      background:rgba(255,255,255,.03);
+      color:var(--muted);
+      font-size:12px;
+      border-bottom:1px solid var(--line);
+    }
+    .trow{
+      border-bottom:1px solid rgba(255,255,255,.06);
+      background:rgba(255,255,255,.01);
+    }
+    .trow:last-child{border-bottom:none;}
+    .pill{
+      display:inline-flex;
+      align-items:center;
+      gap:8px;
+      padding:4px 10px;
+      border-radius:999px;
+      border:1px solid var(--line);
+      color:var(--muted);
+      font-size:12px;
+      white-space:nowrap;
+    }
+    .dot{
+      width:8px; height:8px; border-radius:50%;
+      background:var(--muted);
+    }
+    .dot.low{background:#6ea8ff;}
+    .dot.medium{background:#ffd166;}
+    .dot.high{background:#ff6b6b;}
+    .notes{
+      color:var(--muted);
+      font-size:12px;
+      white-space:nowrap;
+      overflow:hidden;
+      text-overflow:ellipsis;
+    }
+    .empty{
+      margin-top:12px;
+      padding:14px;
+      border-radius:14px;
+      border:1px dashed var(--line);
+      color:var(--muted);
+      background:rgba(255,255,255,.01);
+    }
+    .rightTop{
+      display:flex;
+      flex-direction:column;
+      gap:6px;
+      align-items:flex-end;
+      text-align:right;
+    }
   </style>
 </head>
 <body>
-  <h1>Signals</h1>
-  <div class="muted">Read-only view of captured signals.</div>
+  <div class="topbar">
+    <div>
+      <h1>Signals</h1>
+      <div class="muted">Awareness™ layer — live dashboard from shared memory (no backend).</div>
+    </div>
+    <div class="rightTop">
+      <div class="pill"><span class="dot" id="liveDot"></span><span id="liveStatus">Live</span></div>
+      <div class="small" id="lastUpdated">—</div>
+    </div>
+  </div>
 
-  <div class="card">
-    <div class="row">
-      <button class="secondary" onclick="seed()">Seed demo data</button>
-      <button class="secondary" onclick="clearAll()">Clear</button>
-      <span class="small">Reads from: <code>localStorage.signals</code></span>
-    </div>
-    <div class="list" id="list"></div>
-    <div class="small" style="margin-top:10px;">Auto-refreshes when storage changes.</div>
-  </div>
+  <div class="grid">
+    <div class="card">
+      <div class="muted">Snapshot</div>
+      <div class="kpis">
+        <div class="kpi">
+          <div class="n" id="kTotal">0</div>
+          <div class="l">Total signals</div>
+        </div>
+        <div class="kpi">
+          <div class="n" id="kToday">0</div>
+          <div class="l">Today</div>
+        </div>
+        <div class="kpi">
+          <div class="n" id="kHigh">0</div>
+          <div class="l">High intensity</div>
+        </div>
+      </div>
+
+      <div class="controls">
+        <input id="q" placeholder="Search title / notes…" oninput="render()" />
+        <select id="f" onchange="render()">
+          <option value="all">All intensities</option>
+          <option value="Low">Low</option>
+          <option value="Medium">Medium</option>
+          <option value="High">High</option>
+        </select>
+        <select id="s" onchange="render()">
+          <option value="new">Newest first</option>
+          <option value="old">Oldest first</option>
+        </select>
+      </div>
+
+      <div style="display:flex; gap:10px; margin-top:12px; flex-wrap:wrap;">
+        <button class="secondary" onclick="toggleDemo()">Toggle demo mode</button>
+        <button class="warn" onclick="clearAll()">Clear signals</button>
+      </div>
+
+      <div class="small" style="margin-top:10px;">
+        Data source: <code>localStorage.signals</code> • Updates automatically when Feeder saves.
+      </div>
+    </div>
+
+    <div class="card">
+      <div class="muted">Feed</div>
+
+      <div class="table" id="table" style="margin-top:10px;">
+        <div class="thead">
+          <div>Time</div>
+          <div>Intensity</div>
+          <div>Title</div>
+          <div>Notes</div>
+        </div>
+        <div id="rows"></div>
+      </div>
+
+      <div class="empty" id="empty" style="display:none;">
+        No signals yet. Go to <strong>Feeder</strong>, add one, then come back here — it will appear instantly.
+      </div>
+    </div>
+  </div>
 
   <script>
-    const KEY = "signals";
+    const KEY = "signals";
+    let demo = false;
 
-    function getSignals(){
-      try{
-        return JSON.parse(localStorage.getItem(KEY) || "[]");
-      }catch(e){
-        return [];
-      }
-    }
+    function nowISO(){ return new Date().toISOString(); }
+    function isToday(ts){
+      const d = new Date(ts);
+      const n = new Date();
+      return d.getFullYear()===n.getFullYear() && d.getMonth()===n.getMonth() && d.getDate()===n.getDate();
+    }
+    function fmt(ts){
+      const d = new Date(ts);
+      if (Number.isNaN(d.getTime())) return "—";
+      return d.toLocaleString(undefined, { year:"numeric", month:"short", day:"2-digit", hour:"2-digit", minute:"2-digit" });
+    }
+    function safeArr(x){ return Array.isArray(x) ? x : []; }
+    function loadSignals(){
+      try{
+        const raw = localStorage.getItem(KEY);
+        const arr = raw ? JSON.parse(raw) : [];
+        return safeArr(arr).map(s => ({
+          title: String(s.title ?? "").trim(),
+          notes: String(s.notes ?? "").trim(),
+          confidence: String(s.confidence ?? "Low"),
+          ts: s.ts ?? nowISO()
+        })).filter(s => s.title || s.notes);
+      }catch(e){
+        return [];
+      }
+    }
+    function saveSignals(arr){
+      localStorage.setItem(KEY, JSON.stringify(arr));
+    }
 
-    function render(){
-      const list = document.getElementById("list");
-      const data = getSignals().sort((a,b)=>(b.ts||0)-(a.ts||0));
-      list.innerHTML = data.length ? "" : `<div class="small">No signals yet. Add one in the Feeder.</div>`;
-      data.forEach(s=>{
-        const div = document.createElement("div");
-        div.className="item";
-        div.innerHTML = `
-          <div><span class="pill">${s.confidence||"Low"}</span><strong>${escapeHtml(s.title||"(untitled)")}</strong></div>
-          <div class="small">${new Date(s.ts||Date.now()).toLocaleString()}</div>
-          <div class="muted" style="margin-top:6px; white-space:pre-wrap;">${escapeHtml(s.notes||"")}</div>
-        `;
-        list.appendChild(div);
-      });
-    }
+    function dotClass(conf){
+      const c = String(conf||"").toLowerCase();
+      if (c === "high") return "high";
+      if (c === "medium") return "medium";
+      return "low";
+    }
+    function renderKPIs(data){
+      const total = data.length;
+      const today = data.filter(x => isToday(x.ts)).length;
+      const high = data.filter(x => String(x.confidence).toLowerCase()==="high").length;
+      document.getElementById("kTotal").textContent = String(total);
+      document.getElementById("kToday").textContent = String(today);
+      document.getElementById("kHigh").textContent = String(high);
+    }
+    function norm(s){ return String(s||"").toLowerCase(); }
+    function render(){
+      const q = norm(document.getElementById("q").value);
+      const f = document.getElementById("f").value;
+      const sort = document.getElementById("s").value;
+
+      let data = loadSignals();
+      if (demo) data = seedData().concat(data);
+
+      renderKPIs(data);
+
+      if (f !== "all"){
+        data = data.filter(x => String(x.confidence) === f);
+      }
+      if (q){
+        data = data.filter(x => norm(x.title).includes(q) || norm(x.notes).includes(q));
+      }
+      data.sort((a,b)=>{
+        const at = new Date(a.ts).getTime();
+        const bt = new Date(b.ts).getTime();
+        return sort === "old" ? (at - bt) : (bt - at);
+      });
+
+      const rows = document.getElementById("rows");
+      const empty = document.getElementById("empty");
+      rows.innerHTML = "";
+
+      if (!data.length){
+        empty.style.display = "block";
+        return;
+      }
+      empty.style.display = "none";
+
+      data.slice(0, 200).forEach(s=>{
+        const r = document.createElement("div");
+        r.className = "trow";
+        r.innerHTML = `
+          <div class="small">${fmt(s.ts)}</div>
+          <div><span class="pill"><span class="dot ${dotClass(s.confidence)}"></span>${escapeHtml(s.confidence || "Low")}</span></div>
+          <div><strong>${escapeHtml(s.title || "(untitled)")}</strong></div>
+          <div class="notes" title="${escapeHtml(s.notes || "")}">${escapeHtml(s.notes || "")}</div>
+        `;
+        rows.appendChild(r);
+      });
+      stamp();
+    }
 
-    function seed(){
-      const demo = [
-        {title:"Racing thoughts", notes:"Hard to settle. Might need 2 mins breathing.", confidence:"High", ts:Date.now()-1000*60*8},
-        {title:"Craving spike", notes:"Triggered by stress. Drink water + walk.", confidence:"Medium", ts:Date.now()-1000*60*40},
-        {title:"Stable", notes:"Focus ok. Continue plan.", confidence:"Low", ts:Date.now()-1000*60*120}
-      ];
-      localStorage.setItem(KEY, JSON.stringify(demo));
-      render();
-    }
+    function seedData(){
+      const t = Date.now();
+      return [
+        { title:"Reality check-in", notes:"Quick state check-in. Reset shoulders + breathe.", confidence:"Low", ts: new Date(t-1000*60*6).toISOString() },
+        { title:"Attention drift", notes:"Mind jumping. Reduce inputs, single task.", confidence:"Medium", ts: new Date(t-1000*60*22).toISOString() },
+        { title:"Intensity spike", notes:"Body activated. 90 seconds breathing then water.", confidence:"High", ts: new Date(t-1000*60*55).toISOString() }
+      ];
+    }
 
     function clearAll(){
-      localStorage.removeItem(KEY);
-      render();
+      if (!confirm("Clear all saved signals?")) return;
+      localStorage.removeItem(KEY);
+      render();
     }
 
     function escapeHtml(str){
       return String(str)
         .replaceAll("&","&amp;")
         .replaceAll("<","&lt;")
         .replaceAll(">","&gt;")
         .replaceAll('"',"&quot;")
         .replaceAll("'","&#039;");
     }
 
-    // Auto update when feeder writes
-    window.addEventListener("storage", (e)=>{
-      if(e.key===KEY) render();
-    });
+    function stamp(){
+      document.getElementById("lastUpdated").textContent =
+        "Updated: " + new Date().toLocaleTimeString(undefined, { hour:"2-digit", minute:"2-digit", second:"2-digit" });
+    }
+    function setLive(on){
+      const dot = document.getElementById("liveDot");
+      const txt = document.getElementById("liveStatus");
+      dot.style.background = on ? "var(--accent)" : "var(--muted)";
+      txt.textContent = on ? "Live" : "Paused";
+    }
+    function toggleDemo(){
+      demo = !demo;
+      setLive(true);
+      render();
+    }
+
+    // Live updates: storage event (other tabs) + polling (same tab writes)
+    window.addEventListener("storage", (e)=>{
+      if (e.key === KEY) render();
+    });
+    setInterval(()=>{ render(); }, 2500);
 
-    render();
+    setLive(true);
+    render();
   </script>
 </body>
 </html>
