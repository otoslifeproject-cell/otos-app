# FILE: EMBEDDINGS.md (FULL REPLACEMENT)

## Embeddings Pipeline

- Targets: BRAIN + CORE
- Idempotent via `embedding_hash`
- Stores truncated vector JSON (first 256 dims)
- Manual trigger only
- Safe to re-run

### Local
